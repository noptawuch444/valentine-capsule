# 🚀 Deployment Guide (Thai Edition)

คู่มือการเตรียมตัวนำแอปเลิฟขึ้น Host จริง แบบฟรีและเสถียรครับ

## 1. การเตรียม Database (PostgreSQL)
แนะนำ: **Supabase** หรือ **Neon.tech**
1. สมัครใช้งานและสร้างโปรเจกต์ใหม่
2. ไปที่ส่วน **Database Settings** เพื่อคัดลอก **Connection String** (URL)
3. เก็บค่านี้ไว้เพื่อใส่ในชื่อตัวแปร `DATABASE_URL`

## 2. การเตรียม Backend (Node.js Server)
แนะนำ: **Render.com** (เลือก "Web Service")
- **Root Directory:** `server`
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Environment Variables:**
  - `DATABASE_URL`: (จากขั้นตอนที่ 1)
  - `CLOUDINARY_CLOUD_NAME`: (จาก Cloudinary)
  - `CLOUDINARY_API_KEY`: (จาก Cloudinary)
  - `CLOUDINARY_API_SECRET`: (จาก Cloudinary)
  - `FRONTEND_URL`: (URL ของหน้าเว็บคุณหลังจากขึ้น Vercel แล้ว)

## 3. การเตรียม Frontend (React Web)
แนะนำ: **Vercel**
- **Framework Preset:** Vite
- **Root Directory:** `./` (หรือจุดที่ไฟล์ package.json ของ frontend อยู่)
- **Environment Variables:**
  - `VITE_API_URL`: (URL ของ Backend ที่ได้จาก Render.com แล้วเติม `/api` ต่อท้าย)
    *ตัวอย่าง: https://my-valentine-api.onrender.com/api*

---

### ✅ สิ่งที่ทำไปแล้ว:
1. **vercel.json**: จัดเตรียมไฟล์เพื่อให้ระบบ Routing ของหน้าเว็บเสถียรเมื่อเปิดหน้าย่อยๆ
2. **CORS Config**: ปรับปรุงระบบหลังบ้านให้ยอมรับการเชื่อมต่อจาก URL จริง
3. **Internal API URL**: ตั้งค่าให้หน้าเว็บสามารถสลับระหว่าง Dev และ Production ได้อัตโนมัติ

**คำแนะนำเพิ่มเติม:** 
เมื่อคุณขึ้น Host แล้ว ลำดับคือต้องเอา Backend ขึ้นก่อนเพื่อให้ได้ URL มา แล้วค่อยเอามาใส่ใน Environment Variable ของ Frontend ตอนขึ้น Vercel ครับ!
